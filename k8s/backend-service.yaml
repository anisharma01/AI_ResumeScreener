apiVersion: v1
kind: Service
metadata:
  name: resume-screener-backend-service
  labels:
    app: resume-screener
    component: backend
spec:
  type: ClusterIP
  ports:
  - port: 8000
    targetPort: 8000
    protocol: TCP
    name: http
  selector:
    app: resume-screener
    component: backend
